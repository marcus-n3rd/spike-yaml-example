extends(src='layout.sgr')
  block(name='content')
    header
      h1 Spike YAML Example

    section
      ul
        each(loop="article of articles")
          li {{ article.title }} - {{ article.user }}
